---
layout: layout.njk
title: Ryzokuken's Blog
---
<h1>My Blog</h1>
<ul>
  {%- for post in collections.post reversed -%}
  <li>
    <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
    <h3>📅 {{ post.date | date: "%B %d, %Y" }}</h3>
    {% comment %} <h3>🏷 {{ post.data.tags | slice:1,10 | join: ", "}}</h3> {% endcomment %}
    {% assign tags = post.data.tags | slice:1,10 %}
    <h3>🏷 {%- for tag in tags -%}<a href="/tag/{{tag}}">{{tag}}</a>{%- endfor -%}</h3>
    <p>{{ post.templateContent | truncate: 256 }}</p>
  </li>
  {%- endfor -%}
</ul>

<style>
.container li {
  font-size: 1.5rem;
  margin: 2rem 0;
}

.container h3 > a {
  margin: 0.5rem;
}
</style>
