---
layout: layout.liquid
---
<article class="post-body">
<h1>{{ title }}</h1>
<p class="post-meta"><time datetime="{{ page.date | date: "%Y-%m-%d" }}">{{ page.date | date: "%-d %B %Y" }}</time></p>

{{ content }}
</article>
